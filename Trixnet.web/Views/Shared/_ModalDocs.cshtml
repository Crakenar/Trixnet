@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@*<div class="modal fade" id="QVT" tabindex="-1" role="dialog" aria-labelledby="QVTModal" aria-hidden="true">
    @{
        ViewBag.Title = "Qualité de Vie au Travail";
        ViewBag.Image = "/img/RH/QVT-removebg-preview.gif";
        ViewBag.ID = "QVT";
        //new dictionnary {Title : xxxxx; Image : xxxxx;}
        await Html.RenderPartialAsync("../Shared/_ListDocsModal", pathDirQVT);
    }
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">@ViewBag.Title</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                @if (files.Length > 0)
                {
                    foreach (var file in files)
                    {
                        <i class="fas fa-angle-right trix-blue" style="margin-right:10px"></i>
                        string fileSubbed = file.Substring(Model.Length, file.Length - Model.Length);
                        string link = Model + fileSubbed;
                        link = link.Substring(8, link.Length - 8);
                        <a href="~/@link" target="_blank">@fileSubbed</a>
                        <br />
                        <br />
                        <img src="@ViewBag.Image" />
                    }
                }
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
<div class="col-lg-12">
    <div class="showcase-text" style="padding: 0">
        <div class="SectionHB SectionGD PageSection">
            <div class="titreSection">
                <h2 class="jaapooki text-sections"> QVT </h2>
            </div>
            <a href="#QVT" data-toggle="modal" data-target="#QVT" class="row" style="padding: 10px;">
                <img class="col-lg-10 imgSectionRH" src="@ViewBag.Image">
            </a>
        </div>
    </div>
</div>*@